import React from 'react';
import { Section } from 'react-fullpage';

const Portfolio = () => {
  return (
    <Section>
      <h1>Portfolio</h1>
    </Section>
  );
};

export default React.memo(Portfolio);
