import React from 'react';
import { Section } from 'react-fullpage';

const InfoAbout = () => {
  return (
    <Section>
      <h1>이력서</h1>
    </Section>
  );
};

export default React.memo(InfoAbout);
